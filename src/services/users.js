'use strict';

const users = [];

function getUsers() {
  const temp = users;

  return temp;
}

function getExpenses() {
  const expensesData = [];

  return expensesData;
}

module.exports = {
  getUsers,
  getExpenses,
};
